// HServer BIND example. Copy and customize outside the repository.
// Never commit real customer zones, addresses, transfer keys, or inventories.

zone "example.com" {
    type master;
    file "/etc/bind/zones/example.com.zone";
    allow-transfer { none; };
};
