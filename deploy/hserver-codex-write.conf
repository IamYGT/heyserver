[Service]
# The authenticated admin web terminal is a real root management terminal.
# Disable the service filesystem sandbox so terminal commands can manage the host.
ProtectSystem=false
ProtectHome=false
PrivateTmp=false
NoNewPrivileges=false
ReadWritePaths=
