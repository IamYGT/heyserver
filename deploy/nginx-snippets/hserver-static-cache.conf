# Managed by Heyserver. Dynamic responses are intentionally excluded.
location ~* \.(?:css|gif|ico|jpe?g|js|png|svg|webp|woff2?)$ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800, immutable";
    access_log off;
}
