# Managed by HServer. Prevent common configuration and backup artifacts from being served.
location ~* /(?:\.env|\.git|\.svn|composer\.(?:json|lock)|package-lock\.json)$ {
    deny all;
}

location ~* \.(?:bak|conf|ini|log|sql|sqlite|sqlite3|swp)$ {
    deny all;
}
